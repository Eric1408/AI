#pragma once

#include <vector>
#include <iostream>
#include <fstream>
#include <queue>
#include <stack>

#include "node.h"

typedef std::vector<std::pair<int, float>> path; 

struct Inf {
  int vertices;
  std::vector<path> nodes;
};

class Tree {
public:
  
private:
  
};
